### The two code segments below are each intended to display the average of the numbers in the list One word, num List. Assume that one word, num List contains more than one value.
### ![](https://assets.learnosity.com/organisations/537/914ecc0e-1298-42af-9be1-e7c0d3712322.JPG)
### ![](https://assets.learnosity.com/organisations/537/eb173e42-5701-4e5a-be0c-a1b4f6c063f9.jpg)
### Which of the following best describes the two code segments?

### A) Code segment I displays the correct average, but code segment II does not.
### B) Code segment II displays the correct average, but code segment I does not.
### C) Both code segments display the correct average, but code segment I requires more arithmetic operations than code segment II.
### D) Both code segments display the correct average, but code segment II requires more arithmetic operations than code segment I.

### The answer B is correct.
The second program loops for each num in numlist then adds the sum and each number in the number list. Then outside of the loop it calculates the average, which is correct. The first code is wrong because, while still in the loop the average is calculated which makes it wrong. After all, it calculates each average for each number.

___ 

**Research**


**Loops from list**

Looping from a list is when the loop, loops every item in a list. This allows precise coding of the team, and full control over their movement or behaviour. You can also loop to allow the item to be selected or moved at a given user interaction.

**Key Benefits**

Looping from lists allows us to code individual items in a list rather than the whole list itself. Letting specific coding and correct output performance. 

**Implementation Examples**

For exmple, the list "fruit" is created containg the iteams "apple","banana" and "cherry". Lets say that someone wants to know which pruits you like by printing. Using as loop, you can do that without modifying the list it's self. This si what the code would look like: 

fruits = ["apple","banana","cherry"]

for fruit in fruits:

print(f"I Like to eat {fruit}s.")


